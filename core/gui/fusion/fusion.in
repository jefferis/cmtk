#!/bin/sh

SHELL=/bin/sh
export SHELL

QTDIR=@QTDIR@
export QTDIR

LD_LIBRARY_PATH=$QTDIR/lib:${LD_LIBRARY_PATH}
export LD_LIBRARY_PATH

if [ "$1" = "-d" ]; then
   gdb ./fusion-bin
else
   ./fusion-bin
fi
